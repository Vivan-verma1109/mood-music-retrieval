FUTURE IMPROVEMENTS
===================

DONE

Spotify availability check
- use GET /search to verify final top 10 results exist on Spotify
- skip songs that don't match and fill with the next best candidates
- run at query time, not during playlist export (10 calls, fast) - DONE


Language filtering
- detect language per song (langdetect on lyrics, or Last.fm tags)
- let user specify language preference in query - DONE
- KNOWN BUG: some multilingual songs (e.g. BTS "ON") misclassified as 'en' by langdetect
  because title/partial lyrics are in English — may need better language detection or manual corrections

Genre filtering
- CURRENT: genre keyword detected from query text, matched against alias map in config.py, passed to rerank_by_listeners() as a score boost (2.2x) — not a hard filter
- TODO: replace boost with hard filter using Last.fm track.getTopTags per candidate song
- TODO: remove score boost block from rerank_by_listeners() once hard filter is in
- UI: genre will be a dropdown in the React frontend, passed directly to query() — no query text parsing needed

Expand cluster search
- search top 2 nearest clusters instead of 1
- better coverage for queries that sit on the border between moods
IN PROGRESS

Semantic cluster routing
- replace cluster_tags keyword matching with SBERT embedding of cluster labels/descriptions
- cosine similarity between query embedding and cluster description embeddings to pick clusters
- handles synonyms and paraphrases automatically, no manual keyword lists needed


Artist filter bypasses cluster routing
- if artist is passed, skip cluster routing entirely and use all songs as candidate pool
- filter by artist first, then score by lyric + audio sim directly

NOT STARTED



Rewrite the 13 cluster descriptions — the one high-leverage fix. Feed Claude Code all 13 at once with: make them maximally distinguishable from each other (kill the 6/11 and 1/5 twins), and include usage contexts per cluster (workout/gym for 4, study/focus for 9, party/pregame for 12, late-night driving for 3, etc.). Re-embed, done — it's a config change, no retraining.
Rerun eval_batch on the same 18 queries, rate the new songs that appear (old ratings still count — that's why pooled labels were worth it). Compare against tonight's baseline. Target: context queries (lift heavy, bbq, feels, chill driving) move from 0–25% toward 50%+. That before/after number is the payoff.
While in there: instrumentalness filter for "no lyrics" queries — threshold on a feature you already have, 15 minutes.

Backlog (only if time permits before recruiting peak):

Artist detection in free text (the poison-token fix) — real feature, well-scoped, not urgent
Data-derived cluster embeddings (mean of member lyric embeddings) — the fallback if rewritten descriptions don't move the number
OAuth/playlist export — product polish, zero interview value, lowest priority




Exclude user's liked songs
- Spotify OAuth (Authorization Code flow) to get user's saved tracks
- filter out already-liked songs from results so playlist is always fresh



Demo interface
- React frontend + FastAPI backend
- separate input boxes for mood (text), artist (text), genre (dropdown), language (dropdown), top_k (slider)
- artist filter parsed from query text removed in favor of explicit UI input
- show album art, Spotify links

Spotify playlist export
- OAuth + POST /me/playlists + POST /playlists/{id}/items
- create playlist directly from ranked results

Filter by release year / era
- e.g. "hype songs from the 2000s" → filter candidates to year range before scoring
- dataset may not have release year — fetch via Last.fm track.getInfo or Spotify GET /tracks/{id}

Learned fusion weights
- small supervised layer to tune alpha if labeled mood->playlist data exists

Parallelize Last.fm fetches
- rerank_by_listeners() fetches one artist at a time, bottlenecks on 50 sequential HTTP calls
- use ThreadPoolExecutor to fire all requests concurrently
